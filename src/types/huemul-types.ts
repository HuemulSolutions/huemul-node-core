export enum HuemulEnvironmentType {
  LOCAL = "LOCAL",
  DEV = "DEV",
  QA = "QA",
  PROD = "PROD"
}

export enum DatabaseType {
  firebase = "firebase",
  postgres = "postgres",
  sqlServer = "sql-server",
  bigquery = "bigquery",
}

export enum UserManagerType {
  firebase = "firebase",
  postgres = "postgres",
  sqlServer = "sql-server",
}

export enum CloudProviderType {
  azureFunctions = "azureFunctions",
  azure = "azure",
  google = "google"
}
